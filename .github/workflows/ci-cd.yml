name: Aero Metrics CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          pip install uv
          uv sync --frozen

      - name: Run tests
        run: |
          if [ -d "tests" ]; then
            uv run --with pytest pytest
          else
            echo "No tests directory found. Skipping tests."
          fi

  docker:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build API image
        run: |
          docker build \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/aero-metrics-api:${{ github.sha }} \
            -f Dockerfile .

      - name: Push API image
        run: |
          docker push \
            ${{ secrets.DOCKERHUB_USERNAME }}/aero-metrics-api:${{ github.sha }}

      - name: Build Frontend image
        run: |
          docker build \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/aero-metrics-frontend:${{ github.sha }} \
            -f Dockerfile.frontend .

      - name: Push Frontend image
        run: |
          docker push \
            ${{ secrets.DOCKERHUB_USERNAME }}/aero-metrics-frontend:${{ github.sha }}

  azure-deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: docker
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy API
        run: |
          az containerapp update \
            --name ${{ secrets.AZURE_API_CONTAINER_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --image ${{ secrets.DOCKERHUB_USERNAME }}/aero-metrics-api:${{ github.sha }}

      - name: Deploy Frontend
        run: |
          az containerapp update \
            --name ${{ secrets.AZURE_FRONTEND_CONTAINER_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --image ${{ secrets.DOCKERHUB_USERNAME }}/aero-metrics-frontend:${{ github.sha }}